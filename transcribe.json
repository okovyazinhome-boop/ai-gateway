# curl examples

```bash
curl https://openai.example.com/api/openai/universal \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -H "Content-Type: application/json" \
  --data @examples/chat.json
```

```bash
curl https://openai.example.com/api/openai/universal \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -H "Content-Type: application/json" \
  --data @examples/image.json
```

```bash
curl https://openai.example.com/api/openai/universal \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -H "Content-Type: application/json" \
  --data @examples/transcribe.json
```

```bash
curl https://openai.example.com/api/openai/universal \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -H "Content-Type: application/json" \
  --data @examples/tts.json
```
